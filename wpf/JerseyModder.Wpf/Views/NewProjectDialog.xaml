<Window x:Class="JerseyModder.Wpf.Views.NewProjectDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create New Project" Width="570" Height="340" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
  <Grid Margin="24">
    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
    <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
      <Image Source="pack://application:,,,/Assets/app-icon.png" Width="62" Height="62" Stretch="Uniform" Margin="0,0,16,0" RenderOptions.BitmapScalingMode="HighQuality"/>
      <StackPanel VerticalAlignment="Center">
        <TextBlock Text="Create a jersey project" FontSize="22" FontWeight="SemiBold"/>
        <TextBlock Text="A dedicated folder keeps logos, trims, textures, and exports together." Foreground="{StaticResource MutedBrush}" Margin="0,4,0,0"/>
      </StackPanel>
    </StackPanel>
    <StackPanel Grid.Row="1" Margin="0,0,0,14">
      <TextBlock Text="Project name" FontWeight="SemiBold" Margin="0,0,0,5"/>
      <TextBox x:Name="ProjectNameBox" Text="New Jersey Project" GotFocus="OnSelectAll"/>
    </StackPanel>
    <StackPanel Grid.Row="2">
      <TextBlock Text="Create inside" FontWeight="SemiBold" Margin="0,0,0,5"/>
      <Grid><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
        <TextBox x:Name="ParentFolderBox" IsReadOnly="True" VerticalContentAlignment="Center"/>
        <Button Grid.Column="1" Content="Browse..." Margin="8,0,0,0" Click="OnBrowse"/>
      </Grid>
      <TextBlock x:Name="FolderPreview" Foreground="{StaticResource MutedBrush}" FontSize="11" Margin="0,6,0,0" TextWrapping="Wrap"/>
    </StackPanel>
    <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
      <Button Content="Cancel" IsCancel="True"/>
      <Button Style="{StaticResource PrimaryButton}" Content="Create Project" IsDefault="True" Margin="8,0,0,0" Click="OnCreate"/>
    </StackPanel>
  </Grid>
</Window>
