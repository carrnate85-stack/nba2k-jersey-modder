<UserControl x:Class="JerseyModder.Wpf.Controls.ImageViewport"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid Background="#101518" ClipToBounds="True" SizeChanged="OnSizeChanged">
    <ScrollViewer x:Name="Scroller" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"
                  HorizontalContentAlignment="Center" VerticalContentAlignment="Center"
                  PreviewMouseWheel="OnMouseWheel" PreviewMouseDown="OnMouseDown"
                  PreviewMouseUp="OnMouseUp" PreviewMouseMove="OnMouseMove">
      <Grid x:Name="Stage" Background="Transparent" MouseLeftButtonDown="OnImageClick" MouseRightButtonDown="OnImageRightClick">
        <Image x:Name="Picture" Stretch="Fill" RenderOptions.BitmapScalingMode="HighQuality">
          <Image.LayoutTransform><ScaleTransform x:Name="ZoomTransform" ScaleX="1" ScaleY="1" /></Image.LayoutTransform>
        </Image>
        <Canvas x:Name="Overlay" IsHitTestVisible="False" />
      </Grid>
    </ScrollViewer>
    <Border x:Name="EmptyState" Background="#101518">
      <TextBlock Text="Preview will appear here" Foreground="#809096" HorizontalAlignment="Center" VerticalAlignment="Center" />
    </Border>
    <Border HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="10" Background="#D9182329" CornerRadius="3" Padding="7,4">
      <TextBlock x:Name="ZoomLabel" Text="100%" Foreground="White" FontSize="11" />
    </Border>
  </Grid>
</UserControl>
